# DEVLOG — Sales Data Analytics Tool

## 2026-09-04 — Session 16: V7 Phase 5 — spec-driven chart renderer (v6.09)
- **Did:** Implemented PRD **Phase 5** in `index.html`. Charts are now data, not code: added to the **Charts** module `TYPES`/`BUCKETS` enums, `fieldColumn` (concept/header/substring column resolution), `validateSpec(spec, mapping)` returning `{valid, errors}` and rejecting an invalid spec (unknown type, unknown x/y field, unknown agg, a numeric agg on a dimension/identifier/geo/time column, unknown splitBy, bad sort/limit/overlay) so it never reaches the renderer, `groupRows` (time-bucketed or categorical x), `aggForRows`, and `buildSeries(spec, rows, mapping, opts)` which pulls values from the Phase 4 metric engine and returns Chart.js-ready `{type, labels, datasets}` — supporting time buckets, categorical x, multiple y, `splitBy` (one dataset per distinct value), overlays (regression, movingAverage), and sort/limit. App layer: a "Spec-driven charts" panel with a builder form (type / x field / x bucket / y field / y agg / split / title) that writes the same validated spec, and saved-chart management (add/remove/reorder ↑↓/duplicate ⧉/pin ✕) with each chart drawn to its own canvas through the shared renderer; saved charts re-render against the current filter bus on every `renderAll`. Header bumped to **v6.09 (self-contained)**.
- **Verification:** `.verify-core.js` grew Phase 5 blocks 5.1–5.6 (invalid-spec rejection for unknown type/field/agg and for a sum over an identifier; six chart types produce correct monthly series from a fixture; splitBy yields one dataset per distinct value; regression overlay adds a dataset without altering the base; table type + sort/limit; categorical x grouping). All prior checks reproduce exactly; `node .verify-core.js` and `node .verify-inline.js` both pass. Real-Chrome e2e (`e2e-phase5.js`) loaded Historical_Data, opened the chart builder, rendered a bar chart to a canvas, grew the list, and confirmed an invalid spec shows a validation error — with **zero console errors**.
- **Errors found & fixed:** (1) my splitBy test expected 2 datasets but the implementation correctly produces one per distinct split value (DE/FR/AT = 3) — fixed the assertion, not the code. (2) None in app code; the builder path already routes through the single validated renderer so malformed specs are caught before `new Chart`.
- **Loop traps hit:** 0.

## 2026-09-04 — Session 15: V7 Phase 4 — declarative metric engine (v6.08)
- **Did:** Implemented PRD **Phase 4** in `index.html`. Added a declarative, traceable metric engine on the `Metrics` module: `AGGS` + `applyAgg` (sum/avg/median/count/countDistinct/min/max/rate); a safe expression parser for computed fields (`evalExpr`, no eval / no new Function, operators + - * / % and scalar functions abs/round/floor/ceil/sqrt/pow/min/max); concept/header field resolution (`columnIndex` with a substring fallback so "stage"→OpportunityStage, "completed"→Completed); `measure` returning a traceable object `{value, formatted, definition, agg, field, computed, rowsUsed, filterState, datasetId}`; `concentration` (top-N share + Herfindahl); `comparePeriods`/`filterByRange`/`periodBounds` (current / prior / one-year-earlier with absolute + % deltas); `bucketSeries` + `seasonalityOf`; and `metricPacks(kind)` + `computeMetric(def, rows, mapping, opts)` which returns `{available:false, reason}` when a required field is absent instead of inventing a value. Metric packs by kind: transactions (total units, avg order value, period growth, linear trend, seasonality, top-5 concentration), opportunities (open pipeline, avg deal size, win rate), campaigns (spend, leads, cost per lead), activities (volume, completion rate). Wired the engine into the Overview tab as a per-dataset "Metric pack" chip list. Header bumped to **v6.08 (self-contained)**.
- **Verification:** `.verify-core.js` grew Phase 4 blocks 4.1–4.10 (every agg; expression parser + malformed expression must throw; traceable measure fields; computeMetric; columnIndex concept + substring; concentration top-share + Herfindahl; comparePeriods with a missing prior period does not throw and yields null deltaPct on a zero prior; bucketSeries/seasonality; a missing-field metric reports unavailable; and a real-data regression where `totalUnits` on Historical_Data = 9,537 = the v6 anchor). All prior checks reproduce exactly; `node .verify-core.js` and `node .verify-inline.js` both pass. Real-Chrome e2e (`e2e-phase4.js`) loaded Historical_Data, applied the mapping (4,849 rows, KPI 9,537), opened the Overview tab, and confirmed the metric pack chips render ("Total units: 9.5K", concentration chip) with **zero console errors**.
- **Errors found & fixed:** (1) my first e2e/verify expectation for `round(price*0.9)` was wrong (9.999*0.9 = 8.9991 → 9, not 10) — fixed the assertion. (2) `concentration` with a 2-product fixture and topN=5 covers all values (topShare=1) — changed the fixture to topN=1 so the top-share math is meaningful. (3) Real bug worth noting: `new Date("2024-03-01")` parses as UTC then shifts to Feb 29 in this negative-offset timezone, so raw string dates in the metric engine could land in the wrong month bucket. Fixed by parsing time with the column's detected format via `parseDateByFormat` (local time) in `parseTime`, with an ISO/yyyymmdd auto-detect fallback when no format is declared.
- **Loop traps hit:** 0.

## 2026-09-04 — Session 14: Designed PRD Phase 11 + PLAN Chunk 7 (AI-guided multi-file discovery & onboarding)
- **Did:** Scoped and documented a new capability the user flagged as high-value: the tool reviews a whole folder of data files (e.g. `Activity.csv`, `SalesLines.csv`, `Sales.csv`, `Products.csv`, `Opportunity.csv`, `Leads.csv`, `Contacts.csv`) at once and, via the existing AI endpoint, proposes how each should be mapped, how they join, and what should be analyzed, then presents a **Review Board** for the human to confirm or adjust before anything loads. Added **Phase 11** to `PRD.md` (folder/batch intake via `webkitdirectory`; a deterministic local pre-profile per file; a bounded corpus-summary payload of headers/types/≤5 samples/counts/date ranges — never full rows; a strict-JSON blueprint of dataset kinds, role/concept corrections, a join graph, and an analysis plan; a Review Board with Accept-all / Accept-inferred / Reject plus per-item confirm; persistence by file-shape hash; graceful fallback to the local profile when AI is unconfigured) plus 4 new acceptance checks. Added **Chunk 7** to `PLAN.md` mirroring the phase, and refreshed the `CONTEXT.md` phase list to include Phase 11.
- **Why:** It is the batch, cross-file upgrade of the Phase 1 AI assist and Phase 3 join proposals, and the human-confirmation gate keeps the "AI is additive, never load-bearing" and "source data is the authority" constraints intact.
- **Verification:** Read-only docs edit; no SalesCore/`.verify-*` changes, so no harness re-run was needed. `PRD.md`, `PLAN.md`, `CONTEXT.md` reviewed after edit.
- **Errors found & fixed:** none — but a follow-up review question revealed the initial Phase 11 spec was written from filenames only, not the file contents. I then inspected the actual sample corpus (read headers + row counts of all seven CSVs) and discovered it is a contact-centric CRM/ERP snowflake: Contacts (dimension) ← Leads ← Activity ← Opportunity, plus Contacts ← Sales ← SalesLine ← Product. That surfaced two design gaps and I updated Phase 11 to close them: (1) extend the Phase 2 concept vocabulary to cover contact/lead/sale/sale-line (the Phase 2 set only had product/account/rep/territory/geography/campaign/opportunity/activity/time/amount); (2) support multi-hop transitive join paths (revenue per contact via SaleLine→Sale→Contact), not just pairwise edges. Also noted the corpus's ID-label pairs (ItemNo/ItemName, ContactID/FullName), text columns (Activity.Subject/Notes, Leads.Notes), four time columns, and City/State/Country geo hierarchy.
- **Loop traps hit:** 0.

## 2026-09-04 — Session 9: Versioning scheme change to v6.XX increments + in-flight wait warning (user request)
- **Did:** Per user instruction, reverted the header tag from "v7" back to a v6-based scheme and adopted incremental versioning: while any v7 phase is still pending, each verified change bumps the tag by .01 — this build is **"v6.01 (self-contained)"** (the date-range filter + CSV export shipped in session 8). The tag becomes "v7" only when ALL v7 phases are complete; policy recorded in CONTEXT.md so future sessions don't re-deliberate it. Also changed the in-flight AI status note from the old "local models can take 15s+ to warm up…" line to: "It may take about a minute just to load the model, and several minutes to analyze large data sets. Please wait — do not close or refresh this page."
- **Verification:** Real-Chrome e2e re-run with two new assertions: header shows v6.01; the in-flight status entry (captured via MutationObserver on first Analyze) contains both "about a minute" and "several minutes". ALL E2E CHECKS PASSED, zero console errors; `.verify-core.js`/`.verify-inline.js` unaffected (no SalesCore changes).
- **Errors found & fixed:** none.
- **Loop traps hit:** 0.

## 2026-09-04 — Session 8: v7 date-range filter + CSV export of filtered view (PRD §4 polish)
- **Did:** Implemented the two optional PRD §4 items from CONTEXT.md next steps. (1) Date-range filter: From/To `<input type=date>` in the sidebar; `filteredRows()` now applies both bounds, so chart, KPIs, country bars and the AI payload all respect it; the range is part of `filterKey()`, so changing dates automatically invalidates the cached AI conversation (next Analyze re-sends the FULL DATA payload); "Reset filters" clears the inputs. (2) CSV export: "Export CSV" button on the chart panel downloads exactly the current filtered rows as `Date,Article_ID,Country_Code,Sold_Units` (ISO dates, sorted by date/product/country) via a client-side Blob download with an ISO-dated filename — data never leaves the browser. Header tag bumped to **"v7 (self-contained)"**. Also restored the missing `.env` template (gitignored; empty values only).
- **Verification:** Headless `.verify-core.js` + `.verify-inline.js` still pass unchanged. New real-Chrome puppeteer e2e (`e2e-v7.js`, kept in session state, not committed): 4,849 rows / KPI 9,537 baseline; Sep-2017 window → KPI total drops to 260; export blob captured via `URL.createObjectURL` stub and cross-checked against an independent raw-CSV parse — 168 rows, total 260 = KPI = raw sum; AI flow: first request contains FULL DATA, follow-up sends bare question only (caching intact), changing the date filter forces a fresh FULL DATA resend; reset restores 9,537 and clears inputs; zero console errors. ALL E2E CHECKS PASSED.
- **Errors found & fixed:** none in app code — three e2e test bugs fixed: wrong column index when summing raw CSV (4th vs 5th field); CDP `Page.setDownloadBehavior`/download-dir polling unreliable for blob downloads on file:// → switched to stubbing `URL.createObjectURL` and reading the Blob via FileReader inside the page; the stub's fake `blob:capture` URL legitimately logs a console error, so that one message is filtered from the zero-errors assertion (documented in-test).
- **Loop traps hit:** 0.


## 2026-09-02 — Session 1: Scaffolding + loop-trap guardrails
- **Did:** Created the five core project files (PRD.md, .instructions.md, DEVLOG.md, CONTEXT.md, .env template) plus PLAN.md mapping build sessions in chunks. Added a mandatory "Loop-Trap Detection & Handling" section to `.instructions.md` after an earlier thinking-loop stall: no identical repeated calls without new info, max 2 failures per path before switching strategy, ≤5 tool-call steps with verified progress at each boundary, decide-once rule for ambiguities.
- **Why:** The architecture guide requires the five files; loop traps were a real observed failure mode this session and need explicit rules to prevent recurrence.
- **Errors found:** None yet (scaffolding only).
- **Loop traps hit:** 1 — thinking-loop stall before scaffolding started; handled by stopping, re-scoping to file creation, and codifying the guardrails in `.instructions.md`.

## 2026-09-02 — Session 1 (cont.): App build, verification, GitHub push
- **Did:** Built `index.html` (single-file, no build step; PapaParse + Chart.js via CDN): CSV file picker + drag-drop, column validation with clear errors, YYYYMMDD→Date parsing, time-series chart with product/country filters and day/week/month aggregation, KPI cards (total, top/bottom product, trend slope, seasonality), sales-by-country bars, AI "Analyze" panel (configurable base URL/key/model in UI, localStorage-persisted, temperature slider, editable system prompt) with statistical fallback when unconfigured or on failure. Verified headlessly against `Historical_Data.csv` via `.verify-core.js`: 4849 rows parsed clean; day/week/month bucket sums all equal the grand total (9537); top product 3448 (1163 units), bottom 1579 (317); by-country SE:3469 / FR:2865 / FI:1873 / AT:1330; monthly trend slope +15.33/month; seasonality peak November (+46.8% vs daily avg). Created GitHub repo `joenobk/sales-analytics-tool` and pushed branch `main`.
- **Why:** Fulfills PRD functional requirements 1–5 and PLAN.md chunks 1–5 in one pass; headless verification proves the parse/aggregate/stats/AI-prompt logic without a browser.
- **Errors found & fixed:** (1) week-bucket keys used `Date.getTime()` embedded in a composite string — replaced with clean epoch-ms keys + separate label function, fixing sort order for weeks spanning month boundaries; (2) one arrow-function slipped into the ES5-style SalesCore block causing a SyntaxError under Node eval — rewrote as `function` expressions.
- **Loop traps hit:** 0 (guardrails held: each failed check got exactly one diagnostic pass before fix).

## 2026-09-02 — Session 2: Fix "CSV won't load" + secrets question
- **Did:** Root-caused the silent no-data symptom to the CDN `<script src>` tags (PapaParse/Chart.js) failing when `index.html` is opened via `file://` offline or with a blocked CDN — `window.Papa` undefined, so file selection never produced data and no error surfaced. Fix: inlined both libraries into `index.html` (now fully self-contained, zero CDN refs), plus added a `window.onerror` banner and a library-presence check so any future load failure shows an explicit error instead of silent emptiness. Verified headlessly: inlined libs eval cleanly and expose `Papa`/`Chart`; the exact browser path (`Papa.parse` with `header:true, skipEmptyLines`) parses all 4849 rows clean through SalesCore; day/week/month sums still equal grand total 9537.
- **Why:** PRD requires "open directly" usability and <2s interactive load without a network dependency for core function.
- **Errors found & fixed:** (1) CDN scripts not loading on `file://` → inlined libs; (2) my own inline script used `String.replace`, whose `$` pattern expansion corrupted PapaParse's minified source (it contains `$&`) — switched to `split/join`; caught by a syntax check, logged here per loop-trap rule 5.
- **Loop traps hit:** 1 — thinking-loop stall reported by user mid-fix; handled by stopping, re-scoping to the single failing step (inline + verify), and completing it without retrying failed diagnostics.

## 2026-09-02 — Session 3: Confirmed & fixed the real load bug (user still saw no data)
- **Did:** User reported CSV *still* not loading after session 2's CDN fix, so I stopped theorizing and reproduced in a real browser: puppeteer-core + system Chrome, `file://` URL, driving the actual `<input type=file>` exactly as a user does. That surfaced the true root cause my headless tests had missed: the PapaParse `complete()` callback assigned to an **undeclared** variable (`parsed = ...`), throwing `ReferenceError: parsed is not defined` inside the async FileReader callback — no UI update, and in v1 no error banner existed, hence "no data, no error". (My earlier headless checks passed because they called `Core.parseCsvData` directly, bypassing `handleFile`; jsdom also can't run PapaParse's File-streaming path.) Fix: declared the variable, wrapped the handler in try/catch so any future load failure surfaces in the banner, added a visible **"v2 (self-contained)"** tag to the header so users can tell copies apart, and made drag-drop work anywhere on the page.
- **Verification:** Full real-Chrome `file://` end-to-end: 4,849 rows displayed; KPI total 9,537; top product 3448; 15 product checkboxes; filters recompute (1,035 for single-product view); month aggregation OK; AI fallback renders "STATISTICAL SUMMARY…"; chart canvas has drawn pixels; **zero console errors**; screenshot captured.
- **Errors found & fixed:** undeclared `parsed` (the actual bug) · jsdom FileReader can't instantiate cross-realm (`new fr()` threw) — switched to real Chrome via puppeteer-core instead of fighting the environment · my session-2 root-cause claim ("CDN blocked on file://") was incomplete: even with libs loaded, this ReferenceError would have produced the identical symptom. Corrected in DEVLOG here per rule 5.
- **Loop traps hit:** 0 (each failing test got one diagnostic pass before changing approach; jsdom→Chrome switch logged as a strategy change, not a retry).

## 2026-09-02 — Session 4: v3 feature pass (AI questions, multi-country, trendlines, product buttons, favicon)
- **Did:** Added five requested features to index.html (now tagged **"v3 (self-contained)"**): (1) freeform question textarea in the AI panel — sent with the data summary as part of the user message; when no API is configured the fallback echoes the question and notes that an API must be configured for a written answer; (2) country filter converted from single-select to a multi-select checklist (same pattern as products); (3) trendline overlay options on the chart — regression line (least-squares fit, dashed red) and centered moving average with selectable window (5/10/20 periods), implemented in SalesCore as linearTrendLine / movingAverage; legend appears only when overlays are active; (4) "All"/"None" mini-buttons for the product checklist; (5) orange five-pointed star favicon (SVG data-URI, works on file://) plus the same star rendered inline in the app header.
- **Verification:** Extended the real-Chrome puppeteer e2e test with checks for every new feature — ALL PASSED: 4,849 rows; KPI total 9,537; favicon link + header star present; v3 tag shown; 4 country checkboxes (AT-only filter → 1,330); deselect-all → 0 / select-all → 9,537; chart has 3 datasets with both overlays on; AI fallback echoes the typed question; zero console errors. Headless .verify-core.js and .verify-inline.js still pass.
- **Errors found & fixed:** none — all features verified first run in real Chrome.
- **Loop traps hit:** 0.

## 2026-09-02 — Session 4b: Regression trendline fix (user-reported)
- **Did:** User screenshot showed the regression "line" rendered as a single steep segment pinned to the left edge instead of spanning the chart. Root cause: Chart.js connects dataset points in order, so a 2-point dataset ([yFirst, yLast]) drew one line from bucket 0 to bucket 1 only — not across all buckets. Fix: emit one point per bucket (interpolated along the least-squares fit) so the dashed red regression line spans the full x-axis; also removed its endpoint dots for a cleaner look.
- **Verification:** Extended e2e now asserts the regression dataset has exactly one point per bucket (772 daily buckets in this data). ALL REAL-BROWSER CHECKS PASSED, zero console errors.

## 2026-09-02 — Session 5: v4 AI history, markdown rendering, conversation caching
- **Did:** (1) AI panel now keeps a scrollable answer history — newest on top, each entry shows the question + rendered answer; the data/KPI/chart sections stay visible while scrolling through history. (2) Answers render as real markdown via inlined marked 12.0.2 (no CDN): headings, lists, code blocks, blockquotes, and tables; every table is wrapped in a horizontal-scroll container so wide tables scroll instead of breaking layout. (3) Conversation caching: the app keeps the data summary + Q/A thread client-side keyed by the current filter set — follow-up questions are sent WITHOUT re-sending the data summary as long as filters are unchanged; changing any filter automatically starts a fresh context, and a "New analysis" button forces one manually.
- **Verification:** Extended real-Chrome e2e (stubbed fetch to return a markdown table): history entry created with question on top · markdown answer rendered as a real <table> inside an overflow-x:auto wrapper · newest-first ordering · follow-up request's last user message is the bare question (no new summary) · New analysis clears history. ALL REAL-BROWSER CHECKS PASSED, zero console errors; .verify-core.js and .verify-inline.js still pass.
- **Errors found & fixed:** none in app code — two e2e test bugs fixed (stale #aiOutput selector from v3; fetch-stub config must go through localStorage since the app reads settings on click).

## 2026-09-02 — Session 6: v5 full-data visibility for the AI (user-reported)
- **Did:** User reported the model answering "the summary only gives aggregate totals — it doesn't break down product 332 by month/country or isolate France-specific trends." Root cause: uildAiPrompt sent only pre-computed aggregates, so the LLM genuinely lacked per-product/per-country monthly detail. Fix: added SalesCore.buildFullDataPayload(rows) which emits the COMPLETE filtered dataset at month x product x country grain (nonzero cells only) as a markdown table, plus per-product and per-country totals. The first request now sends the aggregate summary AND this full payload; follow-ups still reuse the cached context (no re-send). Also updated the default system prompt to state the model has the complete dataset at monthly grain and must answer from it rather than claiming it only has aggregates.
- **Sizing:** For Historical_Data.csv the full payload is 406 nonzero cells ≈ 11.5 KB ≈ ~2,900 tokens — comfortably within any OpenAI-compatible context window (e.g., gpt-4o-mini 128k).
- **Verification:** Extended real-Chrome e2e now asserts the first request contains "FULL DATA" + the month/product/country table header with >300 data rows, and that follow-ups still send only the bare question. ALL REAL-BROWSER CHECKS PASSED, zero console errors; .verify-core.js / .verify-inline.js still pass.
- **Errors found & fixed:** none in app code — one e2e sizing probe initially used a wrong month key (day-level) inflating the estimate to 36k tokens; corrected to true monthly grain (~2,900 tokens).

## 2026-09-02 — Session 7: v6 in-flight progress indicator (user-reported)
- **Did:** User reported that during analysis the only feedback was a static "Analyzing…" line, which is unhelpful with local models (15s+ warm-up, then long processing; ~1 min total with full data). Added an in-flight status entry at the top of the AI history: animated spinner + phase label ("Preparing…" → "Analyzing… (N KB sent to model)" → "Receiving response…") + a live elapsed timer ticking every 500ms (e.g. "· 42s"), plus a muted note explaining that local models take 15s+ to warm up so the wait is expected. The Analyze button is disabled and relabeled "Analyzing…" while in-flight, preventing double-submits; on failure the elapsed time is included in the error line (e.g. "[AI request failed after 73s: …]").
- **Honesty note:** a non-streaming chat/completions call returns no intermediate status — headers arrive only when the model finishes — so the timer + payload size is the truthful "still working" signal; we do not fake finer-grained phases.
- **Verification:** Extended real-Chrome e2e with a 1.2s-delayed stubbed fetch: spinner present, phase shows "Analyzing… (N KB sent to model)", elapsed timer ticking ("· 1s"), button disabled + relabeled while in-flight; all prior checks still pass. ALL REAL-BROWSER CHECKS PASSED, zero console errors.
- **Errors found & fixed:** none in app code — two e2e test bugs fixed (stub recorded the call after its delay so follow-up assertions saw 1 call; progress snapshot taken before first timer tick).

## 2026-09-04 — V7 PRD integration
- **Did:** Integrated the complete attached V7 remodeling plan into PRD.md, retaining all eleven phases, tool/chart contracts, phase tests, and final acceptance checks. Preserved legacy regression anchors and UX requirements; reconciled offline dependencies, schema flexibility, query-based AI, missing periods, manual fallbacks, and provenance. Corrected harness references to the existing hyphenated filenames.
- **Project context:** Updated .instructions.md and CONTEXT.md for phased V7 delivery; added a comment to the ignored .env without changing settings. Application code is unchanged and remains V6.
- **Verification:** Both node .verify-core.js and node .verify-inline.js passed; legacy 4,849 rows and 9,537 units confirmed. All Phase 0–10 headings and acceptance checks present; git diff --check passed.
- **Errors found & fixed:** Initial edit command failed before execution because nested backticks conflicted with its JavaScript wrapper; switched to an explicitly quoted command. No files were changed by the failed attempt.
- **Loop traps hit:** 0 after reading project rules; initial shell-tool startup was polled repeatedly before those rules were available.
- **Next:** Implement Phase 0, then Phase 1, with growing test coverage and passing gates before each subsequent phase.
- **Publication status:** Files saved locally. Commit/push was blocked by automatic approval review because remote publication was not explicitly authorized; no commit or push completed.

## 2026-09-04 — Session 10: V7 Phase 0 (refactor & harden) shipped as v6.02
- **Did:** Implemented PRD Phase 0 in index.html per the expanded V7 PRD: (1) restructured SalesCore into eight named modules on the core namespace — Schema, Store, Metrics, Charts, Insight, Text, Report, AI — with every legacy top-level name kept as an alias so existing harnesses pass unchanged; (2) fixed all three v6 defects: no inline event-handler attributes remain in app source (buildFilters and renderCountries now use createElement/textContent/addEventListener/dataset), and markdown output passes through a new inline HTML sanitizer Text.sanitizeHtml that strips script/style tags, on* attributes, and javascript: URLs; (3) added a light observable state container Store.create({get,set,update,subscribe}) holding allRows/products/countries/selectedProducts/selectedCountries/aggMode/dateFromMs/dateToMs/aiCtx — the App IIFE now reads filters from the store instead of the DOM, and `state` is reduced to just the live Chart.js instance. New helpers: Charts.buildLineSpec(series,{regression,movingAvg,maWindow}), Report.buildCsvString(rows), AI.filterKey(products,countries,aggMode,dateFromMs,dateToMs). Bumped header tag v6.01 → **v6.02 (self-contained)** per the versioning policy.
- **Verification:** Grew both harnesses per PRD ("tests grow with the code"): .verify-core.js now has 15 checks (added: all 8 modules present, sanitizer strips script/onclick/javascript:, escapeHtml exact output, store get/set/subscribe, buildCsvString header+sort+empty, AI filterKey stability/sensitivity, buildLineSpec labels/datasets) — ALL PASSED with every legacy regression anchor reproducing exactly (4,849 rows; 9,537 total; top 3448=1,163; bottom 1579=317; SE/FR/FI/AT = 3,469/2,865/1,873/1,330; day/week/month sums agree at 772/112/26 buckets; slope +15.33; November +46.8%). .verify-inline.js now also asserts zero inline event handlers in app source and all 8 modules exported — PASSED. Real-Chrome e2e (puppeteer-core, file://): 16/16 checks passed including v6.02 tag, KPI total, date filter, CSV export blob content + totals, AI context caching/reset, wait-warning text, zero console errors.
- **Errors found & fixed:** Two harness-side bugs in my new Phase 0 tests (not app code): buildCsvString test initially passed raw arrays but the function expects parsed row objects → rewrote with {date,articleId,countryCode,units} fixtures; buildLineSpec test used wrong series shape ({t,v}) and asserted a non-existent spec.data wrapper → corrected to {label,units} buckets and direct labels/datasets assertions. Both caught on first run, fixed once, re-ran green.
- **Loop traps hit:** 0 — each failing check got one diagnostic pass before the fix; no repeated identical tool calls.
- **Next:** Phase 1 (schema inference layer) per PRD: infer column roles from headers/values, confirm with user before analysis, keep the fixed 4-column path as fallback; bump tag to v6.03 when verified.

## 2026-09-04 — Session 11: V7 Phase 1 (schema inference layer) shipped as v6.03
- **Did:** Implemented PRD Phase 1 in index.html: (1) `Schema.inferMapping` classifies every column from ≤1,000 sampled values into a type (date/number/boolean/categorical/identifier/text) and derives a role (time/dimension/measure/identifier/text/geo/ignore) — numeric IDs are never measures; date detection covers yyyymmdd, ISO, m/d/y vs d/m/y disambiguation (both ≥80% → ambiguous for user confirmation), month-year, quarter, and Excel serials (≥25569 only, so small integers stay numbers); currency strings parse via `parseNumberValue` (symbols, thousands separators, %, parens-as-negative). (2) A mapping panel (`#mappingPanel`) shows inferred type/role per column with editable dropdowns plus samples/null/distinct counts; nothing loads until the user clicks **Apply & Load**. (3) Confirmed mappings persist in localStorage keyed by `mappingKey` — an order-insensitive FNV-1a hash of the normalized header set — so re-loading a file with the same shape auto-applies instantly. (4) Optional **AI assist** button sends only headers + inferred types + 5 samples per column (never full data) and applies a JSON role proposal for review. (5) `parseWithMapping` produces exactly the legacy row shape `{date, articleId, countryCode, units}`, so all downstream modules are untouched; broken mappings report which columns failed instead of loading silently. Bumped header tag v6.02 → **v6.03 (self-contained)** per the versioning policy.
- **Verification:** Grew both harnesses: .verify-core.js now 19 checks (added: parseDateByFormat across all 7 formats + invalid rejection, ID-shaped integer classified identifier not measure, currency string parsing, full inferMapping on the real header with correct type/role per column, mapping round-trip through a localStorage stub, mappingKey order-independence, parseWithMapping broken-mapping error reporting) — ALL PASSED with every legacy regression anchor reproducing exactly (4,849 rows; 9,537 total; top 3448=1,163; bottom 1579=317; SE/FR/FI/AT = 3,469/2,865/1,873/1,330; slope +15.33; November +46.8%). .verify-inline.js still PASSED (zero inline handlers, all modules exported). Real-Chrome e2e grew to 20 checks: v6.03 tag · mapping panel appears on first load with correct inferred values (Date=date/time, Sold_Units=number/measure) · Apply loads 4,849 rows + KPI 9,537 · second load auto-applies stored mapping without showing the panel — plus all prior v6/v7 checks and zero console errors.
- **Errors found & fixed:** (1) `inferColumn` initialized `col.role = "ignore"` but never called `assignRole`, so every column came back role-less → added `col.role = assignRole(name, col)` before return; caught by check 17. (2) New Phase 1 functions weren't in the api export block (`Core.parseDateByFormat is not a function`) → added all aliases including TYPE_OPTIONS/ROLE_OPTIONS; the missing options arrays were only caught later by a real-Chrome console error (`Cannot read properties of undefined (reading 'forEach')`), which also proved file:// localStorage persists across reloads in this Chrome profile. (3) Test-fixture bug: Excel serial for 2017-08-17 is 42964, not 42935 — verified against the 1899-12-30 epoch and fixed the fixture.
- **Loop traps hit:** 1 (e2e panel check failed twice: first on a stale `tbody` selector assumption, then on display-state vs classList; each failure produced new information before switching strategy).
- **Next:** Phase 2 (entity/concept resolution) per PRD — resolve dimension values to canonical entities with a confirmation UI built on the Phase 1 mapping panel pattern; bump tag to v6.04 when verified.

## 2026-09-04 — Session 12: V7 Phase 2 (entity/concept resolution) shipped as v6.04
- **Did:** Implemented PRD Phase 2 in index.html with a new **Entities** module on SalesCore: (1) concept binding maps every column to one of ten concepts (product/account/rep/territory/geography/campaign/opportunity/activity/time/amount) via role + synonym table + learned localStorage overrides; identifier columns default to product so ID-only files still report the entity as present. (2) `pairIdLabel` detects ID ↔ label pairs by name pattern (`*_id/*_number/*_code/*_no` with `*_name/*_desc/*_title`) plus cardinality — a few IDs with competing labels are still paired because that is exactly what conflict review exists for, but broadly many-to-many mappings (>25% of IDs conflicted) and geo/time/measure columns are excluded. (3) Per-concept entity registry groups rows by ID (IDs stay strings end-to-end so leading zeros survive load + join), displays the label when one exists, and surfaces any ID with >1 label as an explicit conflict in a new "Entity review" card on the mapping panel — resolved only by user pick (persisted per header shape via `salesConflict:*` keys) or named rule (`most-frequent`/`most-recent`), never silently. (4) Fuzzy matching normalizes names (case fold, punctuation, corporate suffixes, token sort) and scores bigram Dice similarity: ≥0.92 auto-accepts, 0.75–0.92 goes to a human review queue in the same card ("Same entity" confirms; confirmed aliases persist across files). Hierarchy suggestions (child values coinciding with single parents) are offered for confirmation only. (5) Data coverage is first-class: a "Concept coverage" card shows present/partial/absent concepts per file, and `buildAiPrompt` now appends a DATA COVERAGE section naming absent concepts so the model says "cannot be computed" instead of speculating. Bumped header tag v6.03 → **v6.04 (self-contained)** per the versioning policy.
- **Verification:** Grew both harnesses: .verify-core.js adds Phase 2 blocks — pair detection, leading-zero distinctness ("00123" ≠ "123"), conflict surfacing with row counts + most-frequent/most-recent resolution rules, fuzzy review queue + persisted alias applying to a second file, coverage ABSENT reporting + schema payload naming missing concepts and forbidding estimation — ALL PASSED with every legacy regression anchor reproducing exactly (4,849 rows; 9,537 total). .verify-inline.js still PASSED. Real-Chrome e2e grew to 30 checks including a dedicated `phase2_fixture.csv` section: new header shape opens the mapping panel, Article_ID → Article_Name pair detected, X-9 conflict surfaced (not silent) and manually resolved with persistence across reloads, 5 rows load with "00123" and "123" distinct in the product filter, coverage correct for both files — ALL E2E CHECKS PASSED, zero console errors.
- **Errors found & fixed:** (1) `pairIdLabel`'s original strict one-to-one check rejected pairing whenever any ID had two labels — i.e., it silently dropped exactly the conflict cases Phase 2 must surface → replaced with "collect competing labels per ID; reject only when broadly many-to-many". (2) Country_Code was being paired with Article_Name because geo columns were still candidates → excluded geo/time/measure from pairing. (3) `renderEntityReview` referenced an undefined `fm` variable in its empty-state check → use the already-loaded `fuzzyState.queue`. (4) `commitLoaded` was called without the `fields` argument on both load paths, so `lastLoad.mapping` had no header names and coverage/AI-prompt wiring saw nothing → pass fields through; also `renderCoverage` now binds concepts itself since stored mappings don't carry them.
- **Loop traps hit:** 1 (coverage panel showed all-absent twice: first the missing-fields call, then unbound mapping — each failure produced new information before switching strategy).
- **Next:** Phase 3 per PRD.md; bump tag to v6.05 when verified.

## v6.07 — "Edit mapping" re-assignment for loaded datasets (user request)
- **Did:** Users could only assign field roles at first load — stored mappings then auto-applied silently on re-import, so the assignments could never be revised. Added an **Edit mapping** button on the Data coverage card: `startRemap()` re-opens the assignment panel for the active dataset pre-filled with its current confirmed mapping (roles/types/format shown as-is, with display extras defaulted for stored mappings), so the user revises rather than re-deciding from scratch. Apply & Load re-parses the data with the changed roles via the normal `applyMapping` path and replaces the dataset in place (keyed by header shape + source file, so tabs/joins keep their identity), and the revised mapping overwrites the stored one — future imports of that file shape auto-apply the revised assignments. Works for CSV and workbook sheets alike. Bumped header tag v6.06 → **v6.07 (self-contained)** per the versioning policy.
- **Verification:** `.verify-core.js` / `.verify-inline.js` still ALL PASSED (no core changes). Real-Chrome e2e grew to 58 checks: remap re-opens the panel ("Revising…" message), the table is pre-filled with the current assignments (Date=time, Sold_Units=measure), unassigning Country_Code (role → ignore) re-parses and empties the view (4,849 rows kept, KPI 0), and reassigning it to geo restores KPI 9,537 — all prior checks still pass, zero console errors.
- **Errors found & fixed:** none (first run e2e green).
- **Loop traps hit:** 0.
- **Next:** Phase 4 (metric engine) per PRD.md; bump tag to v6.08 when verified.

## v6.06 — filter UX + light/dark theme (user request)
- **Did:** Three user-requested UI improvements in index.html: (1) **Countries All/None** — the Countries filter group now has All/None mini-buttons (mirroring the existing product ones), wired to check/uncheck + sync the store + re-render. (2) **Product search** — a "Search products…" text box above the product checklist filters the visible list live (case-insensitive substring) without touching checkbox state, so filtering never changes the analysis; the search is re-applied whenever the list rebuilds. (3) **Light/dark mode** — a ☾/☀ toggle button in the header applies `html.light` CSS-variable overrides (new `--code-bg/--pre-bg/--row-alt` variables make markdown/tables readable in both themes), persists to localStorage (`salesTheme`, default dark), and the chart grid/ticks/legend colors now come from the computed CSS variables via `chartTheme()` so charts match the active theme on toggle and reload. Bumped header tag v6.05 → **v6.06 (self-contained)** per the versioning policy.
- **Verification:** `.verify-core.js` and `.verify-inline.js` still ALL PASSED (no core changes). Real-Chrome e2e grew to 53 checks: countries None empties the view (KPI 0) and All restores 9,537; product search narrows the list to "332" and clearing restores all 15 products without changing the KPI; theme defaults dark, toggles to light (class + localStorage), persists across reload (☀ label shown), and toggles back to dark — all prior 45 checks (Phase 1–3) still pass, zero console errors.
- **Errors found & fixed:** e2e assertion expected 16 products after clearing the search but Historical_Data has 15 distinct Article IDs (a script grep had counted the CSV header row "Article_ID" as a 16th); debug run confirmed the app restores all labels correctly — corrected the fixture expectation.
- **Loop traps hit:** 0.
- **Next:** Phase 4 (metric engine) per PRD.md; bump tag to v6.07 when verified.

## 2026-09-04 — Session 13: V7 Phase 3 (multi-dataset registry) shipped as v6.05
- **Did:** Implemented PRD Phase 3 in index.html: (1) **Inline SheetJS (xlsx 0.20.3)** — file input now accepts CSV/XLSX with `multiple`; every sheet with a header row + data becomes a candidate dataset, queued through the existing mapping panel one at a time (auto-apply for stored mappings, panel for new shapes); `raw:false` preserves textual representations so leading zeros survive workbooks. (2) **Dataset registry** (`Datasets` module + app layer): each source gets id/name/sourceFile/loadedAt/rowCount/schema/kind (transactions when time+measure exist, else generic — a hint, never a hard requirement)/mapping (concept-bound)/entity registry/rows; only metadata persists to localStorage (`salesDatasets`), so reloads restore ↻ placeholder tabs that ask to re-load the source; re-loading the same source replaces its record (Key = mappingKey|sourceFile). (3) **Dataset tabs + Overview tab**: Overview shows cross-dataset KPIs (datasets, total rows, total filtered rows, declared joins) and a per-dataset row with kind, rows, filtered count, and which filters were skipped. (4) **Concept joins**: `Joins.propose` (shared concepts ≥25% overlap) surfaces proposals per pair; user Declare → hash index built once (`Joins.buildIndex`), stats via `Joins.evaluate`, exact left-join enrichment via `Joins.enrichColumn` with a live joined-amount-total preview on the from-side dataset. (5) **Global filter bus**: product/country/date filters apply per dataset through mapped concepts (`Entities.columnForConcept`); datasets lacking a column show a visible "no 'X' column — this filter does not reach this dataset" note in the Overview; date range is universal for datasets with a time column. (6) **Lenient parse** (`Schema.parseGeneric`): all roles optional, no rows dropped, missing → null + warnings — non-transactional files load instead of blocking; time format is now resolved from the ACTUAL data (`resolveTimeFormat`, used by both parsers) so a persisted format from another source no longer breaks multi-source loads; view layer guards (aggregate + filteredRows) tolerate null dates without crashing. Bumped header tag v6.04 → **v6.05 (self-contained)** per the versioning policy.
- **Verification:** `.verify-core.js` adds a Phase 3 regression block — data-first format resolution on both parsers (wrong persisted yyyymmdd + ISO data must still parse) — plus the existing Datasets/Joins/parseGeneric blocks; ALL PASSED with every legacy anchor exact (4,849 rows; 9,537 total). `.verify-inline.js` PASSED (11 modules, self-contained). Real-Chrome e2e grew to 45 checks: promotions fixture (no country column) loads leniently (6 rows), three datasets coexist without collision (tabs list all; switching preserves KPI 9,537), dataset metadata persisted, product join proposed → declared with exact enrichment sum **13,734,800** (640×10,700 + 561×8,100 + 526×3,300 + 289×2,100), overview notes "no 'country' column" for the skipped dataset while product filtering still reaches it (3 rows), workbook sheets queue into the mapping panel and become datasets, reload restores ↻ placeholders — ALL E2E CHECKS PASSED, zero console errors.
- **Errors found & fixed:** (1) `rec.mapping` stored the raw mapping without concept binding → joins produced zero proposals and the filter bus saw no columns → store the `bindConcepts` result on each record. (2) Placeholder datasets restored from metadata had no `key`, so re-loading created duplicates → recompute key from schema+sourceFile at restore. (3) Stored-mapping auto-apply path returned before continuing the queue → second workbook sheet never queued → call `nextPendingSource` after auto-commit. (4) Time-format trust bug (the big one): persisted yyyymmdd from Historical_Data broke ISO-dated workbook sheets (null dates → crash) → `resolveTimeFormat` probes the actual data first with fallback to the decided format; also normalized nothing on ingestion (format probing handles ISO/mdy fine). (5) `filteredRows`/`aggregate` crashed on null dates (generic datasets) → guards added. (6) e2e-side: clicked the ghost placeholder tab instead of the data tab; expected promotions filtered count assumed 9999 would survive a product filter (it must not — 3 rows is correct).
- **Loop traps hit:** 0 on the app side; one e2e wait used an incorrect selector (mappingMsg text instead of mappingTable first cell) — fixed with a state dump check, not a blind retry.
- **Next:** Phase 4 (metric engine) per PRD.md; bump tag to v6.06 when verified.
